import 'package:flutter/material.dart';

/// Centralized palette to ensure the UI remains restrained and cohesive.
class AppColors {
  static const Color background = Color(0xFF080809);
  static const Color surface = Color(0xFF111113);
  static const Color textPrimary = Color(0xFFF5F2EC);
  static const Color textMuted = Color(0xFF9C9891);
  static const Color accent = Color(0xFFB37963); // Muted terracotta accent.
}
